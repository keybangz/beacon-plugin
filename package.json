{
  "name": "beacon-opencode",
  "version": "2.1.0",
  "type": "module",
  "description": "Beacon — semantic code search for OpenCode (replaces grep with hybrid search)",
  "author": {
    "name": "keybangz"
  },
  "license": "MIT",
  "main": "./dist/beacon.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/keybangz/beacon-opencode"
  },
  "files": [
    "dist/",
    "command/",
    "config/",
    "scripts/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin",
    "semantic-search",
    "code-search",
    "embeddings",
    "vector-search",
    "bm25",
    "hybrid-search",
    "grep-replacement"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run clean && tsc && cp -r config command scripts dist/",
    "dev": "bun run clean && tsc --watch",
    "test": "vitest",
    "test:shard": "node scripts/shard-runner.cjs",
    "test:report": "node scripts/test-reporter.cjs",
    "type-check": "tsc --noEmit",
    "setup": "node scripts/setup.cjs",
    "prepublishOnly": "bun run build",
    "postinstall": "node scripts/setup.cjs"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "fast-glob": "^3.3.0",
    "hnswlib-node": "^3.0.0",
    "onnxruntime-node": "^1.24.3",
    "picomatch": "^4.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/picomatch": "^4.0.2",
    "glob": "^11.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": false
    }
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.23"
  }
}
